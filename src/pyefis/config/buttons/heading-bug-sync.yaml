type: simple
text: "HDG\nSYNC"
dbkey: TSBTN{id}30
bg_color: "#101418"
fg_color: "#00ffff"
# Momentary: snap the heading bug to the current heading (Garmin push-to-sync).
conditions:
  - when: "CLICKED eq true"
    actions:
      - sync value: "HEADBUG, HEAD"
